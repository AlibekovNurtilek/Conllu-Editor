version: "3.3"
services:
  frontend:
    container_name: frontend_app
    build: .
    ports:
      - "3000:80"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"